{
  "name": "roll20-obs-overlay",
  "version": "0.1.0",
  "description": "Relay Roll20 dice rolls onto an OBS overlay via HTTP POST -> SSE.",
  "type": "commonjs",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node scripts/build-dice.mjs && node --env-file=.env src/server.js",
    "prestart": "node scripts/build-dice.mjs",
    "dice:build": "node scripts/build-dice.mjs",
    "test": "node scripts/parser.test.cjs && node scripts/templates.test.cjs && node scripts/redis.test.mjs",
    "smoke": "node scripts/parser.test.cjs && node scripts/templates.test.cjs && node scripts/redis.test.mjs && node scripts/smoke.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "dependencies": {
    "ioredis": "^5.11.1"
  },
  "devDependencies": {
    "ioredis-mock": "^8.13.1"
  }
}
