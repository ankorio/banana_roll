{
  "name": "roll20-obs-overlay",
  "version": "0.1.0",
  "description": "Relay Roll20 dice rolls onto an OBS overlay via HTTP POST -> SSE.",
  "type": "commonjs",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "prestart": "node scripts/build-dice.mjs",
    "dice:build": "node scripts/build-dice.mjs",
    "test": "node scripts/parser.test.cjs",
    "smoke": "node scripts/parser.test.cjs && node scripts/smoke.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
